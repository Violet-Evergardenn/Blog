# 博客设计分析与开发需求文档（Kinetic Orange 版）

> 对标内容结构：lvy-neko / X-plore（[ivyovo-wiki.tech](https://ivyovo-wiki.tech)）
> 改版目标：内容能力基本一致，视觉语言彻底差异化，并保留二次元气质
> 整理日期：2026/03/18

---

## 一、项目定位与改版原则

本次改版采用 **Kinetic Orange** 数字粗野主义风格，保留 X-plore 的页面信息架构与功能覆盖，但在视觉表达上实现明显区分。

### 1. 内容不变（保持一致）

- 页面类型保持一致：首页、关于、文章列表、文章详情、图片/相册、图片详情、Live2D。
- 核心能力保持一致：文章系统、标签归档、TOC、点赞收藏、评论、相册、灯箱、管理入口。
- 信息组织保持一致：时间线归档、首页多模块聚合、侧栏摘要与目录等。

### 2. 风格变更（明确差异）

- 从「玻璃拟态 + 柔和圆角 + 薄荷渐变」切换为「高对比粗野主义 + 硬边框 + 黑橙白三色」。
- 从「可爱/二次元氛围」切换为「技术感/编辑感/工业感」。
- 从「静态卡片层次」切换为「重排版 + 跑马灯 + 线性旋转 + 结构化动效」。

### 3. 调性平衡（新增）

- 视觉骨架：Kinetic Orange（强对比、重排版、硬边）。
- 气质表达：保留二次元（角色、语气、符号、故事感）。
- 建议比例：`70% Tech Brutalist + 30% ACG Flavor`。
- 原则：二次元味道通过内容与图形表达，不回退到 X-plore 的玻璃拟态语言。

---

## 二、视觉系统（Design Tokens）

### 1. 色彩

- Brand Orange: `#FF4D00`
- Solid Black: `#000000`
- Pure White: `#FFFFFF`
- 暗背景上的次级白：`rgba(255,255,255,0.8)`
- 禁用：柔和粉彩、主背景渐变、低对比灰字

### 2. 字体

- Display / Headline: `Archivo Black`
  - 必须全大写
  - letter-spacing: `-0.04em`
  - line-height: `0.85 ~ 0.9`
- Meta / Label: `Space Mono`
  - letter-spacing: `-0.02em`
- Body: `Inter`

### 3. 边框与形态

- 全局硬边：`2px solid #000000`（暗底区域可用白色 20% 分割线）
- 仅以下元素允许圆角：
  - 胶囊导航
  - 胶囊按钮
  - CTA 圆角按钮
- 其余卡片与容器保持直角

### 4. 动效

- 跑马灯：线性、无限循环、文本驱动
- 旋转指示器：`12s linear infinite`
- 悬停反馈：`translateX(16px)` + `scale(1.1)`（按组件裁剪）
- 降级策略：支持 `prefers-reduced-motion`

### 5. 交互细节

- 文字选中样式：
  - background: `#000000`
  - color: `#FF4D00`
- 交互强调方式：位移、反色、边框反差，不依赖阴影
- 情绪文案允许轻度二次元语气（如状态标签、按钮文案、分区标题副文案）

---

## 三、页面结构（保留内容，重做视觉）

### 1. 首页（Hero + 信息聚合）

#### 保留内容能力

- 导航入口、问候语、社交链接、最新文章、随机推荐、播放器、写文章入口。

#### 新版视觉实现

- 首屏采用全屏 Typographic Hero：`16vw` 主标题（Archivo Black，全大写）。
- 主背景为 `#FF4D00`，文字与边框使用纯黑。
- Hero 下方用 `2px` 横向分割线连接元信息行：
  - 左：Based in / 状态信息
  - 中：旋转 Scroll Down 圆形指示器
  - 右：多行角色标题（创作者/工程师等）
- 顶部使用固定浮动导航：
  - 中央黑色胶囊承载主链接
  - 左 logo，右社交图标
- 首屏后接黑底斜切跑马灯区块，作为视觉节奏过渡。
- 在 Hero 元信息区域加入“角色设定标签”与轻量立绘位（可静态图或 Live2D 缩略入口），保留二次元识别点。

### 2. 关于我页面

#### 保留内容能力

- 个人介绍 Markdown、引用块、GitHub/邮箱、点赞互动。

#### 新版视觉实现

- 采用“海报式”标题区：大字 + 单色强对比。
- 内容区改为硬边框分段，不使用毛玻璃。
- 引用块改为黑底白字，左侧橙色粗线强调。
- 增加“角色档案卡”模块（昵称、属性、技能树、当前状态），强化 ACG 人设表达。

### 3. 文章列表页

#### 保留内容能力

- 年份归档、筛选（日/周/月/年/分类）、已读状态、标签、更多加载。

#### 新版视觉实现

- 从时间线卡片改为“纵向服务清单”风格：
  - 每项顶部细分隔线（白色 20%）
  - 左侧编号（Space Mono，橙色）
  - 中间超大标题（Archivo Black）
  - 下方标签使用胶囊样式
  - 右侧箭头默认隐藏，悬停显示并旋转 `45deg`
- 悬停态：标题右移 `16px`，背景提升到白色 5% 透明层。
- 文章标签可加入“番剧/角色/设定/观后感”等 ACG 维度，保持内容语义上的二次元味道。

### 4. 文章详情页

#### 保留内容能力

- 标题、发布时间、Markdown 富文本、作者信息、摘要、TOC、点赞、编辑入口。

#### 新版视觉实现

- 文章头图区域使用黑橙强对比标题条。
- 主内容采用高可读排版（Inter），限制阅读宽度。
- 侧栏 TOC 做硬边信息面板，当前章节以橙色高亮。
- 代码块采用黑底白字，关键符号橙色点缀。
- 允许在文章头部加入“作品卡片”或“角色注释块”（硬边样式），形成轻量 ACG 叙事层。

### 5. 文章图片灯箱 / 嵌入内容查看

#### 保留内容能力

- 模态查看、左右切换、分页、互动栏（点赞/收藏/评论/分享）。

#### 新版视觉实现

- 灯箱底部工具栏改为黑色硬边条。
- 页码与操作文案使用 Space Mono。
- 焦点按钮悬停使用反色 + 轻位移。

### 6. 图片/相册页

#### 保留内容能力

- 图片集合展示、可重排、内容归类。

#### 新版视觉实现

- 取消拍立得柔和风，改为“编辑墙”布局：
  - 黑底网格基准
  - 图片容器硬边框
  - 局部倾斜区块制造不规则张力
- 可保留随机重排逻辑，但视觉语言统一黑橙白。

### 7. 图片详情页

#### 保留内容能力

- 全屏查看、日期与描述信息。

#### 新版视觉实现

- 图片外框改为粗边硬框。
- 右下信息标签改为黑底白字，关键时间戳用橙色。

### 8. Live2D 页面

#### 保留内容能力

- 独立 Live2D 展示、全站入口。

#### 新版视觉实现

- 页面外框和控制区统一粗边风格。
- 保留角色本体，并作为全站二次元气质核心来源；UI 仍采用硬边风格，避免可爱化组件泛滥。

---

## 四、关键组件规范

### 1. 浮动胶囊导航

- fixed 顶部布局：左 logo / 中间胶囊链接 / 右社交。
- 中间胶囊：黑底、白字（Space Mono 12px）。
- 链接 hover：白底黑字，边界清晰，过渡时长 180ms 左右。

### 2. 旋转 Scroll 指示器

- 直径：`144px`
- SVG 圆形路径 + `<textPath>` 循环文本：`SCROLL DOWN •`
- 字体：Space Mono 9px，bold，uppercase
- 动效：整体容器 `12s linear infinite rotate(360deg)`
- 中心静态箭头：向下

### 3. Brutalist 列表卡（文章/服务项通用）

- 结构：`[序号] [标题+标签] [箭头]`
- 分隔：顶部 `1px`（白色 20%）
- hover：
  - 背景 `rgba(255,255,255,0.05)`
  - 标题 `translateX(16px)`
  - 箭头显示并旋转 `45deg`

### 4. 跑马灯区块

- 容器：黑底，整体 `skewY(-2deg)`
- 双行文本：
  - Row1：橙字，Archivo Black，`10vw`
  - Row2：白字 `80%` 透明，反向滚动
- 速度保持可读，不做突兀加速

---

## 五、与 X-plore 的差异清单（必须满足）

1. 不使用玻璃拟态（移除 blur 卡片与柔和阴影主导）。
2. 不使用薄荷绿渐变背景（改为黑橙白高对比）。
3. 不使用大面积圆角卡片（除胶囊导航与按钮）。
4. 不使用可爱化装饰语言（改为工业化排版和几何构图）。
5. 首屏采用超大字排版主导，而非插画主导。
6. 引入结构化运动（marquee + rotate），弱化装饰性动效。
7. 二次元元素以“角色内容层”承载，而非“玻璃卡片视觉层”承载。

---

## 六、二次元保留清单（不破坏差异化）

1. 保留 Live2D 与角色设定叙事（人物标签、状态语、世界观小字）。
2. 保留动漫向内容分类与标签体系（番剧、角色、设定考据、同人灵感）。
3. 保留轻量拟声词/情绪标注（如 BOOM、LOADING、SYSTEM ONLINE），作为版式点缀。
4. 保留插画或头像资产，但嵌入硬边容器，不使用软阴影大圆角卡片。
5. 保留互动语气的人格化表达（按钮与提示文案），增强亲和而不削弱科技感。
6. 控制上限：单屏二次元装饰元素不超过 2 处，避免视觉噪声。

---

## 七、开发需求清单（Kinetic Orange 版本）

```text
📦 博客开发需求清单（功能与原方案一致，UI 技术栈更新）
├── 🎨 前端 UI
│   ├── Typographic Hero（超大字号 + 全大写控制）
│   ├── Floating Pill Navigation（固定悬浮胶囊）
│   ├── Skewed Marquee Section（双行无缝滚动）
│   ├── Brutalist List Item 组件（编号/标题/箭头 reveal）
│   ├── 统一硬边框系统（2px + 分隔线变量）
│   ├── 响应式版式（桌面/平板/移动）
│   ├── 高对比主题变量（橙/黑/白）
│   └── 选中文本高亮主题（黑底橙字）
│
├── 📄 内容系统
│   ├── Markdown 渲染
│   ├── 文章 CRUD
│   ├── 分类与标签系统
│   ├── 年/月/周/日归档筛选
│   ├── TOC 自动生成与滚动高亮
│   ├── 阅读状态标记
│   └── 摘要提取
│
├── 🖼️ 媒体系统
│   ├── 相册展示与详情查看
│   ├── 灯箱/轮播与分页
│   ├── 媒体上传管理
│   └── 嵌入内容展示
│
├── 🤖 特色功能
│   ├── Live2D 页面与浮动入口
│   ├── 动态问候语
│   ├── 随机文章推荐
│   ├── 点赞/收藏/评论互动
│   └── 社交链接聚合
│
├── 📊 数据展示
│   ├── 浏览量统计
│   ├── 收藏/标记统计
│   └── 最新内容与公告聚合
│
└── 🔧 管理功能
    ├── 在线编辑入口
    ├── 公告发布
    └── 站点状态标签
```

---

## 八、落地优先级（建议）

1. 先做全局 Design Tokens（颜色/字体/边框/动效变量）。
2. 先改首页与导航，建立风格锚点。
3. 再改文章列表与详情，确保阅读体验不受损。
4. 最后迁移相册与 Live2D 页面，统一视觉系统。
5. 全程做可访问性检查（对比度、键盘可达、减少动态支持）。

---

## 九、验收标准

1. 功能覆盖不低于原计划（内容能力完整）。
2. 与 X-plore 视觉感知明显不同（首屏即能识别）。
3. 文章页可读性达标（移动端与桌面端均稳定）。
4. 动效连续且性能稳定（不卡顿、不干扰阅读）。
5. 全站风格统一遵循 Kinetic Orange 规范。
6. 用户可感知二次元气质（角色、人设、语气、内容标签），但整体仍为高对比硬边风。